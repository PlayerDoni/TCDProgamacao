import { AppBar, Toolbar, Typography } from "@mui/material";

function Header() {
  return (
    <AppBar>
      <Toolbar>
        <Typography variant="h6">
          Rei dos Celulares - Gestão de Conteúdo
        </Typography>
      </Toolbar>
    </AppBar>
  );
}

export default Header;
